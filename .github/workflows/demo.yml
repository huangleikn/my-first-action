steps:
  # 步骤1：检出代码（必备操作）
  - name: Checkout Code
    uses: actions/checkout@v4   # 官方Action，拉取仓库代码

  # 步骤2：打印系统信息
  - name: Show OS Info
    run: uname -a

  # 步骤3：列出仓库文件（验证代码已拉取）
  - name: List Files
    run: ls -la
